{
  "name": "ig-auto-publish",
  "version": "1.0.0",
  "private": true,
  "description": "Publishes card-news carousels to Instagram via the Meta Graph API, run from GitHub Actions.",
  "main": "publish.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "publish": "node publish.js"
  }
}
